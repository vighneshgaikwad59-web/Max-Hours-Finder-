A simple Python script that takes a list of workers along with the hours
they've worked, and returns the worker who has put in the maximum hours.
<br>
<hr>

🚀 Possible improvements


Handle ties (two workers with the same max hours)
Add input validation (e.g., empty list, negative hours)
Take input from a CSV file instead of a hardcoded list
Rename variables (montly → top_worker, worker → hrs) for clarity


